{
  "_comment": "In ~/.claude/settings.json unter \"hooks\" einfuegen. <BIN> = absoluter Pfad zur Standalone-App (z.B. /Applications/claude-monitor oder C:/.../claude-monitor.exe). Ohne Binary, mit Node-Quelle: 'node <PFAD>/index.js' statt '<BIN>'.",
  "hooks": {
    "UserPromptSubmit": [
      { "hooks": [ { "type": "command", "command": "<BIN> event working" } ] }
    ],
    "PreToolUse": [
      { "matcher": "*", "hooks": [ { "type": "command", "command": "<BIN> event working" } ] }
    ],
    "Notification": [
      { "hooks": [ { "type": "command", "command": "<BIN> event waiting" } ] }
    ],
    "Stop": [
      { "hooks": [ { "type": "command", "command": "<BIN> event idle" } ] }
    ]
  }
}
